<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib"
                    mc:Ignorable="d">

    <Style x:Key="BaseStyle" TargetType="Control">
        <Setter Property="FontSize" Value="{DynamicResource FontSize}" />
        <Setter Property="FontFamily" Value="{DynamicResource FontFamily}" />
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="BaseTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="{DynamicResource FontSize}" />
        <Setter Property="FontFamily" Value="{DynamicResource FontFamily}" />
        <Setter Property="Foreground" Value="{DynamicResource White}" />
        <Style.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.5" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="PopupBorder" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource AlphaBlack}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AlphaWhite}" />
        <Setter Property="BorderThickness" Value="{DynamicResource PopupBorderThickness}" />
        <Setter Property="CornerRadius" Value="{DynamicResource MainCornerRadius}" />
    </Style>


    <!-- Daze Theme Stuff -->

    <!-- Transparent TextBox -->
    <Style x:Key="TextBoxTransparent" TargetType="TextBox" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Height" Value="32" />
        <Setter Property="BorderThickness" Value="{DynamicResource ControlBorders}" />
        <Setter Property="Background" Value="{DynamicResource FullAlpha}" />
        <Setter Property="Foreground" Value="{DynamicResource White}" />
    </Style>

    <!-- Transparent ComboBox -->
    <Style x:Key="ComboBoxTransparent" TargetType="ComboBox" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="Foreground" Value="{DynamicResource White}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Background" Value="{DynamicResource AlphaWhite}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AlphaBlack}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ControlBorders}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBox}">
                    <Border x:Name="Border" BorderThickness="0"
                        CornerRadius="{DynamicResource MainCornerRadius}"
                        Background="{DynamicResource AlphaWhite}">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Focusable="False" ClickMode="Press"
                                Margin="0,0,2,0"
                                IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <DockPanel LastChildFill="False" Background="Transparent">
                                            <Viewbox DockPanel.Dock="Right" Width="25">
                                                <TextBlock Text="6" FontFamily="Marlett"
                                                    Foreground="{DynamicResource White}"
                                                    VerticalAlignment="Center"/>
                                            </Viewbox>
                                        </DockPanel>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                Margin="5,0,25,0" IsHitTestVisible="False"
                                Content="{TemplateBinding SelectionBoxItem}"
                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" />
                            <TextBox x:Name="PART_EditableTextBox" Visibility="Hidden"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Stretch" Margin="0,0,25,0" Focusable="True"
                                BorderThickness="0" Background="{TemplateBinding Background}"
                                IsReadOnly="{TemplateBinding IsReadOnly}"
                                Foreground="{DynamicResource White}">
                            </TextBox>
                            <Popup x:Name="Popup" IsOpen="{TemplateBinding IsDropDownOpen}"
                                Placement="Bottom" Focusable="False" AllowsTransparency="True">
                                <Grid MaxHeight="{TemplateBinding MaxDropDownHeight}"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}">
                                    <Border BorderBrush="{DynamicResource FullAlpha}"
                                        BorderThickness="{DynamicResource PopupBorderThickness}"
                                        CornerRadius="{DynamicResource MainCornerRadius}"
                                        Padding="5,5,5,5" Margin="0,5,0,0"
                                        Background="{DynamicResource AlphaBlack}">
                                        <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                            VerticalScrollBarVisibility="Auto">
                                            <StackPanel IsItemsHost="True"
                                                KeyboardNavigation.DirectionalNavigation="Contained" />
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource White}"
                                TargetName="Border" />
                        </Trigger>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter Property="IsTabStop" Value="False" />
                            <Setter Property="Visibility" Value="Visible"
                                TargetName="PART_EditableTextBox" />
                            <Setter Property="Visibility" Value="Hidden" TargetName="ContentSite" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Text box without borders -->

    <Style x:Key="TextBoxNoBorders" TargetType="TextBox" BasedOn="{StaticResource BaseStyle}">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Height" Value="32" />
        <Setter Property="BorderThickness" Value="{DynamicResource NoBorders}" />
    </Style>
    
    <!-- Checkbox Style -->

    <Style x:Key="CheckBoxStyle" TargetType="Border">
        <Setter Property="CornerRadius" Value="{DynamicResource MainCornerRadius}" />
        <Setter Property="BorderThickness" Value="{DynamicResource ControlBorders}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AlphaWhite}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource White}" />
            </Trigger>
            <Trigger Property="IsFocused" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource White}" />
            </Trigger>
        </Style.Triggers>
    </Style>                  

</ResourceDictionary>