<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <Style TargetType="{x:Type SearchBox}">
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="MinHeight" Value="24" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type SearchBox}">
                    <Grid>
                        <ContentControl x:Name="PART_SeachIcon"
                                        ContentTemplate="{DynamicResource SearchTextIconTemplate}"
                                        VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0"
                                        Foreground="{DynamicResource White}" />
                        <TextBox x:Name="PART_TextInpuText" Background="{DynamicResource AlphaWhite}" CaretBrush="{DynamicResource White}"
                                 VerticalContentAlignment="Center" Padding="7,0,25,0" FontSize="{DynamicResource FontSize}" Style="{StaticResource TextBoxNoBorders}" Foreground="{DynamicResource White}">
                                 <TextBox.Resources>
                                    <Style TargetType="{x:Type Border}">
                                      <Setter Property="CornerRadius" Value="{DynamicResource MainCornerRadius}"/>
                                    </Style>
                                  </TextBox.Resources>
                                 </TextBox>
                        <ContentControl x:Name="PART_ClearTextIcon"
                                        ContentTemplate="{DynamicResource ClearTextIconTemplate}" 
                                        FontSize="18" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,15,0" Cursor="Arrow" 
                                        Foreground="{DynamicResource White}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>