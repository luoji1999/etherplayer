<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="DetailsViewItemStyle" TargetType="{x:Type ListBoxItem}">
        <Setter Property="Foreground" Value="{DynamicResource AlphaBlack}" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="FontSize" Value="{DynamicResource FontSizeLarge}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                    <Grid >
                        <!-- <Border x:Name="SelectedRectangle" IsHitTestVisible="False" Visibility="Collapsed"
                      BorderThickness="6,0,0,0" BorderBrush="{DynamicResource White}" Margin="0,4,0,0" />
                        <Border x:Name="HoverRectangle" IsHitTestVisible="False" Visibility="Collapsed" 
                         BorderThickness="6,0,0,0" BorderBrush="{DynamicResource White}" Margin="0,4,0,0" /> -->
                         
                        <Border x:Name="SelectedItem" Padding="{TemplateBinding Padding}" Margin="5,5,5,5" CornerRadius="{DynamicResource MainCornerRadius}" Cursor="Arrow">
                            <ContentPresenter />
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsInstalled}" Value="True">
                            <Setter Property="Foreground" Value="{DynamicResource White}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsInstalled}" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource LightGrey}" />
                        </DataTrigger>                        
                        <Trigger Property="IsSelected" Value="True">
                            <!-- <Setter Property="Visibility" Value="Visible" TargetName="SelectedRectangle" /> -->
                            <Setter Property="Foreground" Value="{DynamicResource AlphaBlack}" />
                            <Setter Property="Background" Value="{DynamicResource White}" TargetName="SelectedItem" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <!-- <Setter Property="Visibility" Value="Visible" TargetName="SelectedRectangle" /> -->
                            <Setter Property="Background" Value="{DynamicResource AlphaWhite}" TargetName="SelectedItem" />
                            <Setter Property="Foreground" Value="{DynamicResource White}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>