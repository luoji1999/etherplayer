<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="GridViewItemStyle" TargetType="{x:Type ListBoxItem}">
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="Opacity" Value="1" />
        <Setter Property="Tag" Value="{DynamicResource UnselectedGamesOpacity}" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="0.95" ScaleY="0.95"></ScaleTransform>
            </Setter.Value>
        </Setter>
        
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}" >
                    <Grid Background="{DynamicResource FullAlpha}" Margin="{Settings ItemSpacingMargin}" x:Name="ItemGrid" Cursor="Arrow">
                        <Border x:Name="SelectionBorder" BorderThickness="3" Margin="-3" CornerRadius="{DynamicResource MainCornerRadius}" Tag="{DynamicResource SelectedGameBorder}" />
                        <ContentPresenter />
                    </Grid>

                    <ControlTemplate.Resources>
                        <Storyboard x:Key="ScaleInStoryboard">
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="0:0:0.1">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseIn" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="0:0:0.1">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseIn" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>

                        <Storyboard x:Key="ScaleOutStoryboard">
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="0.95" Duration="0:0:1.2">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="0.95" Duration="0:0:1.2">
                                <DoubleAnimation.EasingFunction>
                                    <CubicEase EasingMode="EaseOut" />
                                </DoubleAnimation.EasingFunction>
                            </DoubleAnimation>
                        </Storyboard>
                </ControlTemplate.Resources>

                    <ControlTemplate.Triggers>
                        <MultiTrigger>
                            <Setter Property="Opacity" Value="1" />
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsSelected" Value="False" />
                            </MultiTrigger.Conditions>

                            <MultiTrigger.EnterActions>
                                <RemoveStoryboard BeginStoryboardName="scaleOut1" />
                                <RemoveStoryboard BeginStoryboardName="scaleIn2" />
                                <BeginStoryboard x:Name="scaleIn1" Storyboard="{StaticResource ScaleInStoryboard}" />
                            </MultiTrigger.EnterActions>
                        </MultiTrigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource White}" TargetName="SelectionBorder"/>
                            <Setter Property="Effect" TargetName="ItemGrid">
                                <Setter.Value>
                                    <DropShadowEffect ShadowDepth="6" BlurRadius="20" Opacity="0.4" Direction="270" />
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Opacity" Value="1" />
                            <Trigger.EnterActions>
                                <RemoveStoryboard BeginStoryboardName="scaleOut1" />
                                <BeginStoryboard x:Name="scaleIn2" Storyboard="{StaticResource ScaleInStoryboard}" />
                            </Trigger.EnterActions>
                        </Trigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="False" />
                                <Condition Property="IsSelected" Value="False" />
                            </MultiTrigger.Conditions>
                            
                            <MultiTrigger.EnterActions>
                                <RemoveStoryboard BeginStoryboardName="scaleIn2" />
                                <BeginStoryboard x:Name="scaleOut1" Storyboard="{StaticResource ScaleOutStoryboard}" />
                            </MultiTrigger.EnterActions>
                            <Setter Property="BorderThickness" Value="0" TargetName="SelectionBorder"/>
                        </MultiTrigger>

                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="True"/>
                                <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="False"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Opacity" Value="{DynamicResource UnselectedGamesOpacityAmount}" />
                        </MultiDataTrigger>

                        <DataTrigger Binding="{Binding ElementName=SelectionBorder, Path=Tag}" Value="False">
                            <Setter Property="BorderThickness" Value="0" TargetName="SelectionBorder"/>
                        </DataTrigger>

                        <DataTrigger Binding="{Settings ShowGridItemBackground}" Value="False">
                            <Setter Property="Background" Value="Transparent" TargetName="ItemGrid" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>
</ResourceDictionary>