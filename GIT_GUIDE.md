# Git 版本管理指南（ETPlayer 项目）

本文档面向项目开发者与自动化助手，介绍如何使用 Git 进行版本管理，替代"复制整个文件夹"的笨重做法。

---

## 为什么用 Git？

| 以前的做法 | 用 Git 之后 |
|---|---|
| 改代码前复制整个项目到另一个文件夹 | 提交保存快照，随时可回退 |
| 改坏了要手动把文件夹内容复制回来 | 一键恢复到任意历史版本 |
| 不知道改了哪些文件 | 精确显示每个文件的差异 |
| 多个版本占用大量磁盘空间 | Git 只存增量，几乎不占额外空间 |

---

## 在 VSCode 中使用 Git（图形化操作，无需命令行）

### 打开源代码管理面板

点击 VSCode **左侧边栏的第三个图标**（分支形状的图标），或按快捷键 **`Ctrl+Shift+G`**，即可打开"源代码管理"面板。

---

### 一、查看修改了哪些文件

打开源代码管理面板后，你会看到：

```
源代码管理 (SOURCE CONTROL)
├── 更改 (Changes)          ← 修改过但还没提交的文件列表
│   ├── M  SomeFile.cs      ← M = 修改 (Modified)
│   ├── U  NewFile.cs       ← U = 新增 (Untracked)
│   └── D  DeletedFile.cs   ← D = 删除 (Deleted)
```

**点击任意文件** → 会打开"差异对比视图"，左边是旧版本，右边是新版本，改动部分会高亮显示。

---

### 二、提交代码（保存当前状态）

这是最常用的操作，相当于"给当前状态拍个快照"：

1. 在源代码管理面板中，查看"更改"列表里的文件
2. 点击文件右边的 **`+`** 号，将文件添加到"暂存的更改"（或点击"更改"标题右边的 `+` 一次性全部添加）
3. 在顶部的 **消息输入框** 中输入本次改动的说明，例如：`修复启动崩溃问题`
4. 点击 **`✓ 提交`** 按钮（或按 `Ctrl+Enter`）

```
┌─────────────────────────────────┐
│  修复启动崩溃问题                  │  ← 在这里输入提交说明
├─────────────────────────────────┤
│  ✓ 提交                          │  ← 点击提交
├─────────────────────────────────┤
│  暂存的更改 (Staged Changes)      │
│    M  source/Playnite/App.cs     │  ← 已暂存，会被提交
├─────────────────────────────────┤
│  更改 (Changes)                   │
│    M  source/Playnite/Other.cs   │  ← 未暂存，不会被提交
└─────────────────────────────────┘
```

> **提示**：如果你想一次提交所有修改，可以直接输入消息后点击提交按钮旁边的下拉箭头 `▼`，选择 **"提交全部"**。

---

### 三、创建分支（在改代码前做）

分支 = 一条独立的开发线路。在分支上改代码不会影响主分支。

1. 点击 VSCode **左下角状态栏** 上显示的分支名（比如 `master`）
2. 在弹出的菜单中选择 **"创建新分支..."**
3. 输入分支名，例如：`feature/添加XX功能`，按回车
4. VSCode 会自动切换到新分支，左下角会显示新的分支名

```
VSCode 左下角状态栏：
┌──────────────────────────────────────────┐
│  ⎇ feature/添加XX功能   ← 当前所在分支     │
└──────────────────────────────────────────┘
```

---

### 四、切换分支

1. 点击 VSCode **左下角** 的分支名
2. 在弹出列表中选择要切换到的分支（比如 `master`）
3. 文件内容会自动变成那个分支的版本

---

### 五、合并分支（改完后把代码合并回主分支）

1. 先切换到 `master` 分支（点击左下角分支名 → 选择 `master`）
2. 按 **`Ctrl+Shift+P`** 打开命令面板
3. 输入 `Git: 合并分支` 或 `Git: Merge Branch`
4. 选择要合并进来的分支（比如 `feature/添加XX功能`）
5. 合并完成！

---

### 六、撤销修改（改坏了想恢复）

#### 撤销单个文件的修改（还没提交的）
- 在源代码管理面板中，找到那个文件
- 点击文件右边的 **↩️ 撤销图标**（弯箭头）
- 文件会恢复到上次提交时的状态

#### 撤销所有修改（还没提交的）
- 在"更改"标题右边，点击 **↩️ 撤销所有更改**

#### 回退到之前的某次提交
- 按 **`Ctrl+Shift+P`** 打开命令面板
- 输入 `Git: 撤消上次提交` 或 `Git: Undo Last Commit`
- 这会撤销最近一次提交，但保留文件修改（你可以重新编辑后再提交）

---

### 七、查看提交历史

VSCode 内置的历史查看比较简单，推荐安装扩展 **Git Graph**：

1. 按 `Ctrl+Shift+X` 打开扩展商店
2. 搜索 **`Git Graph`** 并安装
3. 安装后，在源代码管理面板顶部会出现一个 **图表图标**，点击即可看到漂亮的提交历史图

没有扩展的话，也可以：
- 按 `Ctrl+Shift+P` → 输入 `Git: View History` 或 `Git: 查看历史`
- 或在终端中运行 `git log --oneline`

---

### 八、删除不需要的分支

1. 按 **`Ctrl+Shift+P`** 打开命令面板
2. 输入 `Git: 删除分支` 或 `Git: Delete Branch`
3. 选择要删除的分支

> **注意**：不能删除当前所在的分支，需要先切换到其他分支。

---

## 推荐的日常工作流程（VSCode 版）

### 每次让 Roo 做较大改动时：

```
步骤 1：左下角点击分支名 → 创建新分支 → 输入名称（如 feature/本次改动）
         ↓
步骤 2：让 Roo 开始改代码
         ↓
步骤 3：改完后，Ctrl+Shift+G 打开源代码管理
         → 点击 + 暂存所有文件
         → 输入提交说明
         → 点击 ✓ 提交
         ↓
步骤 4：构建测试（运行 build.bat）
         ↓
    ┌────┴────┐
  成功？      失败？
    ↓           ↓
步骤 5a：     步骤 5b：
切换回 master   左下角切换回 master
合并分支        （改动自动消失，回到安全状态）
删除功能分支     可以删除失败的分支
```

---

## 分支命名建议

| 前缀 | 用途 | 示例 |
|---|---|---|
| `feature/` | 新功能 | `feature/添加游戏分类` |
| `fix/` | 修复 bug | `fix/修复启动崩溃` |
| `ui/` | 界面调整 | `ui/调整主界面布局` |
| `experiment/` | 实验性改动 | `experiment/尝试新方案` |

---

## 提交说明建议

写清楚改了什么，方便以后回溯：

- ✅ `修复启动时数据库路径为空导致的崩溃`
- ✅ `添加游戏分类功能：支持自定义标签`
- ✅ `调整主界面侧边栏宽度和颜色`
- ❌ `更新代码`（太模糊）
- ❌ `fix`（看不出修了什么）

---

## 推荐安装的 VSCode 扩展

| 扩展名 | 作用 |
|---|---|
| **Git Graph** | 图形化查看提交历史和分支关系 |
| **GitLens** | 在代码行内显示最后修改人和时间 |

安装方法：`Ctrl+Shift+X` → 搜索扩展名 → 点击安装

---

## 注意事项

1. **不用担心构建产物**：项目已有 `.gitignore` 文件，`bin/`、`obj/`、`Debug/`、`Release/` 等目录会被自动忽略
2. **经常提交**：小步提交比一次性大提交更安全，出问题时更容易定位
3. **构建前先清理**：运行 `clean.bat` 清理构建产物后再提交
4. **master 分支保持稳定**：始终在新分支上开发，测试通过后再合并
